{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt-ts",
    "build": "nuxt-ts build",
    "start": "nuxt-ts start",
    "generate": "nuxt-ts generate",
    "deploy": "nuxt-ts generate && yes | rm -r ../src/main/resources/static/* && cp -r dist/* ../src/main/resources/static"
  },
  "dependencies": {
    "@nuxt/typescript-runtime": "^2.0.0",
    "@nuxtjs/axios": "^5.12.2",
    "core-js": "^3.6.5",
    "distinct-colors": "^3.0.0",
    "nuxt": "^2.14.6",
    "nuxt-buefy": "^0.4.3",
    "sass": "^1.35.1"
  },
  "devDependencies": {
    "@nuxt/types": "^2.14.6",
    "@nuxt/typescript-build": "^2.0.3"
  }
}
